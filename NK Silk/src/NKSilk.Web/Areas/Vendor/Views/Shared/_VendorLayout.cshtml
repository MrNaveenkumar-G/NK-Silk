@{
    string Active(string controller) =>
        string.Equals(ViewContext.RouteData.Values["controller"]?.ToString(), controller, StringComparison.OrdinalIgnoreCase)
            ? "active" : "";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] · NK Silk Seller</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/admin.css" asp-append-version="true" />
</head>
<body class="admin-body">
    <div class="admin-shell">
        <aside class="admin-sidebar">
            <div class="admin-brand">NK<span class="text-accent">Silk</span> <small>Seller</small></div>
            <nav class="nav flex-column">
                <a class="nav-link @Active("Dashboard")" asp-area="Vendor" asp-controller="Dashboard" asp-action="Index">Dashboard</a>
                <a class="nav-link @Active("Products")" asp-area="Vendor" asp-controller="Products" asp-action="Index">My Products</a>
                <a class="nav-link @Active("Inventory")" asp-area="Vendor" asp-controller="Inventory" asp-action="Index">Inventory</a>
                <a class="nav-link @Active("Orders")" asp-area="Vendor" asp-controller="Orders" asp-action="Index">Orders</a>
                <hr class="text-white-50" />
                <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Index">↩ Back to store</a>
            </nav>
        </aside>
        <div class="admin-main">
            <header class="admin-topbar d-flex justify-content-between align-items-center">
                <span class="fw-semibold">@ViewData["Title"]</span>
                <div class="d-flex align-items-center gap-3">
                    <span class="small text-muted">@User.Identity?.Name</span>
                    <form asp-area="" asp-controller="Account" asp-action="Logout" method="post" class="m-0">
                        <button class="btn btn-sm btn-outline-secondary">Logout</button>
                    </form>
                </div>
            </header>
            <main class="admin-content">
                @if (TempData["Success"] is string ok)
                {
                    <div class="alert alert-success py-2">@ok</div>
                }
                @if (TempData["Error"] is string err)
                {
                    <div class="alert alert-danger py-2">@err</div>
                }
                @RenderBody()
            </main>
        </div>
    </div>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
